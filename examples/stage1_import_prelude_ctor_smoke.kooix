import "../stdlib/prelude";
import "../stage1/compiler";

fn main() -> Int {
  let src: Text =
    "import \"../stdlib/prelude\" as prelude; fn mk_some() -> prelude::Option<Int> { prelude::Option::Some(1) }; fn mk_none() -> prelude::Option<Int> { prelude::Option::None() }; fn main() -> Int { 0 };";
  let r: Result<Int, S1Diagnostic> = s1_compile(src);
  match r {
    Ok(_c) => 0;
    Err(_e) => 1;
  }
};

