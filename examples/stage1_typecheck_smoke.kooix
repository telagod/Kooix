import "../stdlib/prelude";
import "../stage1/compiler";

fn main() -> Int {
  let src: Text =
    "fn inc(x: Int) -> Int { x + 1 }; fn main() -> Int { let i: Int = 0; i = inc(i); return i; i };";
  let r: Result<Int, S1Diagnostic> = s1_compile(src);
  match r {
    Ok(_c) => 0;
    Err(_e) => 1;
  }
};

