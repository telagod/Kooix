import "../stdlib/prelude";
import "../stage1/compiler";

fn main() -> Int {
  let src: Text =
    "import \"../stdlib/prelude\" as prelude; fn l() -> Int { prelude::text_len(\"hi\") }; fn main() -> Int { l() };";
  let r: Result<Int, S1Diagnostic> = s1_compile(src);
  match r {
    Ok(_c) => 0;
    Err(_e) => 1;
  }
};

