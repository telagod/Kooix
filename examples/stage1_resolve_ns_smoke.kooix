import "../stdlib/prelude";
import "../stage1/compiler";

fn main() -> Int {
  let src: Text =
    "import \"../stdlib/prelude\" as prelude; enum Option<T> { None; Some(T); }; fn mk(x: Int) -> Option<Int> { Option::Some(x) }; fn len(xs: prelude::List<Int>) -> Int { 0 };";
  let r: Result<Int, S1Diagnostic> = s1_compile(src);
  match r {
    Ok(_c) => 0;
    Err(_e) => 1;
  }
};

