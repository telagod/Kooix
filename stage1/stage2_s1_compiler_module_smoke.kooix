import "../stdlib/prelude";
import "diag";
import "compiler";

fn main() -> Int {
  let src: Text = "fn main() -> Int { let x: Int = 1; x };";
  let r: Result<Int, S1Diagnostic> = s1_compile(src);
  match r {
    Err(_e) => 110;
    Ok(code) => {
      if code == 0 { 0 } else { 111 }
    };
  }
};
